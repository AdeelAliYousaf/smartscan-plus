export default function SettingsPage() {
  return (
    <div className="min-h-screen bg-slate-50 p-8">
      <div className="container mx-auto">
        <h1 className="text-2xl font-bold mb-4">Settings / Admin Profile</h1>
        <p className="text-sm text-slate-600 mb-6">Manage admin account, change password, and system settings.</p>

        <div className="bg-white rounded-2xl p-6 shadow-sm border border-slate-100">
          <p className="text-sm text-slate-500">Placeholder: Admin profile and system settings (scan types, notification email).</p>
        </div>
      </div>
    </div>
  );
}
